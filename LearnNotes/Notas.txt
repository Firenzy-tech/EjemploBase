Para crear los binarios en una carpeta de salida bin: javac -d bin -sourcepath backend\src backend\src\com\ejemplo\Program.java 

Para poder ejecutar la salida de los binarios: java -cp bin com.ejemplo.Program

Borrar binarios: rm -r bin;